/**
 * Shared configuration between server and client
 */

/** Stream key for SSR streaming - must match on both sides */
export const STREAM_KEY = '__APP_STREAM__';

/** CSS selector for the app root element */
export const APP_ROOT = '.app';
