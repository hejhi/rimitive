/**
 * PageService Type
 *
 * Minimal interface that pages require.
 * Both server (parse5) and client (DOM) services satisfy this.
 */
import type { Service } from '../service.js';

export type PageService = Service;
