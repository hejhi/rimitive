---
title: "createInstrumentation() function"
sidebar:
  label: "createInstrumentation()"
  badge:
    text: "fn"
    variant: "tip"
---

<!-- Do not edit this file. It is automatically generated by API Documenter. -->

[Home](../) &gt; [@lattice/lattice](./) &gt; createInstrumentation

## createInstrumentation() function

Create an instrumentation context from configuration.

This is the main entry point for setting up instrumentation. Returns `undefined` if instrumentation is disabled, allowing safe optional chaining in production.

Type overloads provide precise return types: - `enabled: false` → always returns `undefined` - `enabled: true` with providers → always returns `InstrumentationContext` - Otherwise → `InstrumentationContext | undefined`

**Signature:**

```typescript
export declare function createInstrumentation(config: InstrumentationConfig & {
    enabled: false;
}): undefined;
```

## Parameters

<table><thead><tr><th>

Parameter


</th><th>

Type


</th><th>

Description


</th></tr></thead>
<tbody><tr><td>

config


</td><td>

[InstrumentationConfig](./instrumentationconfig/) &amp; { enabled: false; }


</td><td>


</td></tr>
</tbody></table>

**Returns:**

undefined

## Example 1

Basic usage

```ts
import { createInstrumentation, devtoolsProvider } from '@lattice/lattice';

const instrumentation = createInstrumentation({
  enabled: import.meta.env.DEV,
  providers: [devtoolsProvider()],
});

// Use with compose
const ctx = compose(
  { signal: Signal(), computed: Computed() },
  helpers,
  { instrumentation }
);
```

## Example 2

Production-safe pattern

```ts
// Returns undefined in production (enabled: false)
const instrumentation = createInstrumentation({
  enabled: false,
  providers: [],
});

// Type is `undefined`, no runtime overhead
```

## Example 3

Multiple providers

```ts
const instrumentation = createInstrumentation({
  providers: [
    devtoolsProvider(),
    analyticsProvider(),
    loggingProvider({ verbose: true }),
  ],
});
```

