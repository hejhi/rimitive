---
title: "ParentContext type"
sidebar:
  label: "ParentContext"
  badge:
    text: "type"
    variant: "note"
---

<!-- Do not edit this file. It is automatically generated by API Documenter. -->

[Home](../) &gt; [@lattice/view](./) &gt; ParentContext

## ParentContext type

Parent context passed to RefSpec.create() for adapter composition Allows child RefSpecs to know their parent's adapter and element

Note: adapter uses 'unknown' for variance - any Adapter<T> is assignable

**Signature:**

```typescript
export type ParentContext<TElement> = {
    adapter: unknown;
    element: TElement;
};
```

## Example


```typescript
import type { ParentContext } from '@lattice/view/types';

const context: ParentContext<HTMLElement> = {
  adapter: domAdapter,
  element: parentElement
};

// Used internally when creating child elements
const childRef = childSpec.create(svc, {}, context);
```

