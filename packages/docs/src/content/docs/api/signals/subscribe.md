---
title: "Subscribe() function"
sidebar:
  label: "Subscribe()"
  badge:
    text: "fn"
    variant: "tip"
---

<!-- Do not edit this file. It is automatically generated by API Documenter. -->

[Home](../../) &gt; [@lattice/signals](../) &gt; Subscribe

## Subscribe() function

Create a Subscribe service factory.

Subscribe tracks dependencies only from the source function, not the callback. This is useful when you want to react to specific signals without tracking all dependencies used in the callback.

\*\*Most users should use the preset instead:\*\*

```ts
import { createSignals } from '@lattice/signals/presets/core';
const { subscribe } = createSignals();
```

**Signature:**

```typescript
Subscribe: (args_0?: SubscribeOptions | undefined) => import("@lattice/lattice").Service<SubscribeFactory, SubscribeDeps>
```

## Parameters

<table><thead><tr><th>

Parameter


</th><th>

Type


</th><th>

Description


</th></tr></thead>
<tbody><tr><td>

args\_0


</td><td>

[SubscribeOptions](../subscribeoptions/) \| undefined


</td><td>

_(Optional)_


</td></tr>
</tbody></table>

**Returns:**

import("@lattice/lattice").[Service](../../lattice/service/)<!-- -->&lt;[SubscribeFactory](../subscribefactory/)<!-- -->, SubscribeDeps&gt;

## Example 1

Basic subscription

```ts
const { signal, subscribe } = createSignals();

const count = signal(0);

const unsubscribe = subscribe(
  () => count(),
  (value) => console.log(`Count: ${value}`)
);
// logs: "Count: 0"

count(1); // logs: "Count: 1"
unsubscribe();
count(2); // no log
```

## Example 2

Selective tracking

```ts
const count = signal(0);
const multiplier = signal(2);

// Only re-runs when count changes, NOT when multiplier changes
subscribe(
  () => count(),
  (value) => console.log(value * multiplier())
);

count(5);       // logs: 10
multiplier(3);  // no log (multiplier not in source)
count(5);       // no log (value unchanged)
count(6);       // logs: 18
```

## Example 3

vs Effect

```ts
// Effect: tracks ALL dependencies
effect(() => {
  console.log(a() + b()); // re-runs when a OR b changes
});

// Subscribe: tracks only source dependencies
subscribe(
  () => a(),              // only tracks a
  (val) => log(val + b()) // b is NOT tracked
);
```

