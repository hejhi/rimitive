/**
 * View devtools instrumentation
 *
 * Provides instrumentation wrappers for view extensions (el, map)
 * following the same pattern as signals instrumentation.
 */

export { instrumentEl } from './el';
export { instrumentMap } from './map';
