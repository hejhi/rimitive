/**
 * @fileoverview Main entry point - exports vanilla by default
 * 
 * Similar to Zustand's pattern:
 * - Default export is vanilla (framework-agnostic)
 * - /react subpath for React bindings
 */

export * from './vanilla';