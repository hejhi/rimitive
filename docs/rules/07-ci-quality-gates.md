### Continuous Integration (CI) and Quality Gates

- CI runs tests, linters, type checks, security scans, and coverage on every
  push.
- Maintain ≥ 90% statement coverage; never let coverage regress without an
  explicit, reviewed exception.
